# Verification — Spec 213 (#2428)

## Phase 4a — RED, independently re-confirmed

Re-ran all three red-phase files myself against the pre-fix tree (commit `ba172009`): `IngestEventBatchCommandHandlerTests` failed to **compile** (`RefusedEnvelope` and `.Envelope`/`.Reason` did not exist yet — the correct red for a genuinely new type, per ADR-0139's established use elsewhere this session); `PersistenceLoopHostedServiceTests` failed exactly the two predicted cases (`A_batch_refusal_records_the_rule_it_broke_not_the_retry_window`, `A_skewed_envelope_in_a_batch_that_throws_is_rejected_on_its_first_single_attempt`), with the two green-by-design cases already green and `Records_and_releases_a_delivery_that_never_stores` unmodified and green; `DeadLetterReasonIntegrationTests` built clean against types that already existed (no compile dependency on the fix — the new shape lives entirely inside the persistence loop's implementation).

## Phase 4b — GREEN, independently re-confirmed

Re-ran against the post-fix tree: `IngestEventBatchCommandHandlerTests` 11/11, `PersistenceLoopHostedServiceTests` 13/13 (12 from the original red-phase set + US1-F, added after T004 exposed `Because` as `internal`). Full solution `dotnet build -c Release` clean, 0 warnings, 0 errors. Hard constraints independently re-checked against the diff, not taken from the engineer's report: `RecordRejectionAsync` contains zero references to `retry.Value` (the three remaining occurrences in the file are in `ExecuteAsync`, `RetryAsync`, and an options read, none inside `RecordRejectionAsync`); the `:215` sentence (`$"not storable after {window} of retrying"`) is byte-identical to the pre-fix source; `Because` is declared `internal static`.

**US1-F proved by counterfactual, not merely observed green:** temporarily reverted `Because`'s truncation to a bare `RejectionReason.From(text)`, confirmed the new test fails with the exact `ArgumentException` `RecordRejectionAsync`'s catch would otherwise swallow silently (stranding the delivery on `carried` forever — the hazard the fix exists to close), then restored the truncation and confirmed green again. `git diff` on the production file was clean throughout the counterfactual.

## Phase 5 — live end-to-end procedure: deferred to CI, not skipped

`spec.md`'s Independent end-to-end test procedure — including step 7's counterfactual (a `:194` row and a `:215` row present in the **same** `GET /dead-letters` listing with **different** reasons, which distinguishes this fix from one that merely replaced the sentence everywhere) — requires a live Aspire stack. **This was not attempted locally.** Free system RAM measured 3.3GB before this phase began, worse than the levels that led two sibling deliveries today (#2432/spec 211, #2430/spec 212) to defer live verification to CI rather than risk a local boot. `DeadLetterReasonIntegrationTests.cs` (T003) is already written, builds clean, and will run as part of CI's `integration tests (Docker)` job — the same deferral pattern used successfully twice today, both times confirmed by reading the CI job's own log for the specific new test's result rather than trusting the bucket's aggregate conclusion.

**Named precisely, not left implicit:** no single test in this delivery covers step 7's counterfactual on its own. It is covered only by the *joint* green of two tests in the same CI job, against the same stack: `DeadLetterReasonIntegrationTests.A_clock_skewed_delivery_records_the_rule_it_broke_not_the_retry_window` (asserts the `:194` row's reason) and the pre-existing, untouched `PoisonDeliveryEscapeIntegrationTests`'s `error.ShouldContain("not storable")` (asserts the `:215` row's reason). That substitution is legitimate — both already run in the same job — but the pre-merge gate is "read the integration job's own log," and a gate whose target isn't named by test method is not actually readable. **Also worth stating plainly: `DeadLetterReasonIntegrationTests` has never executed anywhere before this PR's CI run.** It is not a re-run of a previously-passing test; CI's run is its first execution ever, so the first read of that log is checking the test's own correctness (the 2-minute poll window, the 30-second settle, the hamburg positive/negative control) as much as it's checking the fix.

**Before merging, the orchestrator will read the integration-tests job's own log** and confirm both named tests above are present and green — including step 3's assertion (the reason contains the typed code, not "of retrying"), step 4's row-count check (exactly one row — proving acknowledgement, not a carried/redelivered delivery), and step 5's pair of assertions (the hamburg operator sees their own hamburg row and does not see the munich row — a positive control alongside the negative one, since an empty or broken listing would satisfy the negative assertion alone without proving the scoping mechanism works). `develop` has no required status checks, so this reading is the actual gate.

**plan.md's Definition-of-Done item 6 ("observed by a person") is explicitly not met by this delivery** — recorded here rather than left to be inferred from the absence of a claim to the contrary.

**What each instrument proves, stated precisely:** the unit-level counterfactual above is what proves the 512-character truncation hazard is actually closed — the integration test doesn't exercise that path (no production `IngestEventError` variant is long enough to reach it). The integration test proves the ordinary case works end-to-end through the real MQTT ingress and Postgres `dead_letters` table, and that fab-scoping still excludes rows the caller doesn't hold (US1's step 5, re-asserted rather than assumed since the reason now embeds device-controlled `occurredAt` text). Neither instrument substitutes for the other.

Latency: **N/A** — argued rather than asserted, per spec.md's §Latency section: `RecordRejectionAsync` runs only for deliveries that will never be stored, and this change does strictly less string formatting on that path than before (composing a reason once versus reconstructing a sentence from a captured `TimeSpan` at every one of the three call sites).
